# Everything the project needs that a Kaggle GPU image does not already ship.
# torch, numpy, scipy and pandas are preinstalled there; pinning them here would
# trigger a slow and risky reinstall inside the notebook, so they are omitted on
# purpose. Install them yourself when running locally.
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
Pillow>=9.0.0
tqdm>=4.60.0
transformers>=4.27.0
open_clip_torch>=2.20.0
PyYAML>=6.0

# Test suite (tests/ is untracked -- see .gitignore).
pytest>=7.0
